## Github Pages Build
This is the build from `main` that is hosted by github pages. 
Main is not directly hosted due to dependencies, requires app to be built and deployed on a separate branch that will be handled by github pages.  
> [!IMPORTANT]
> `main` is all source code, **NOT** what the website is running on.
